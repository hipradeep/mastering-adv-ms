input {
  tcp {
    port => 5000
    codec => json_lines
  }
  file {
    path => "c:/Users/hipradeep/Documents/mastering-adv-ms/logs/*.log"
    start_position => "beginning"
  }
}

output {
  elasticsearch {
    hosts => ["localhost:9200"]
    index => "spring-boot-logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
